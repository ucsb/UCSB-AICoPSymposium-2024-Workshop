CKEDITOR.editorConfig = function( config ) {
    config.uiColor = '#AADC6E';
    config.resize_enabled = true;
    config.disableObjectResizing = false;
    config.allowedContent = "svg[*]; title; path; g; div[*]";
};